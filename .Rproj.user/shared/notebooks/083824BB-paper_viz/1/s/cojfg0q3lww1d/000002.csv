"0","```r
ggplot(weighted_delays, aes(x = income)) + 
  geom_histogram(fill='lightblue', color='black') + 
  xlim(c(0, max(weighted_delays$income))) + 
  scale_x_log10() +
  ylab('Count') + 
  xlab('Average Monthly Income (BRL, log-scale)') + 
  theme(axis.title = element_text(size = 20)) + 
  theme(axis.text = element_text(size = 15))
```"
"1","Scale for 'x' is already present. Adding another scale for 'x', which will replace the existing scale.
"
